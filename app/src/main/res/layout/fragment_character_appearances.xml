<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/light_blue"
    android:padding="10dp"
    android:gravity="center_horizontal"
    tools:context=".screens.character_appearances.CharacterAppearancesFragment">

    <include
        android:id="@+id/tb"
        layout="@layout/thumbnail_episode"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginVertical="10dp"
        android:textStyle="bold"
        android:textColor="@color/blue"
        android:textSize="22sp"
        android:text="@string/character_appearances" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_appearances"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        android:orientation="vertical"
        tools:listitem="@layout/thumbnail_character"
        tools:itemCount="10"/>

</LinearLayout>